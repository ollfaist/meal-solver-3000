{
  "config": {
    "step": {
      "user": {
        "title": "Meal Solver 3000",
        "description": "Enable Meal Solver 3000 to manage the week's dinners."
      }
    },
    "abort": {
      "already_configured": "Meal Solver 3000 is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Rules",
        "data": {
          "max_rules":       "Max per week",
          "min_rules":       "Min per week",
          "no_consecutive":  "No consecutive side dishes",
          "repeat_interval": "Repeat interval (days)"
        },
        "data_description": {
          "max_rules":       "Format: tag:count, tag:count  —  e.g. köttfärs:2, fisk:1, lamm:1",
          "min_rules":       "Format: tag:count, tag:count  —  e.g. vegetarisk:1",
          "no_consecutive":  "Comma-separated list  —  e.g. potatis, ris, pasta, nudlar",
          "repeat_interval": "Number of days before the same dish can be randomized again (0 = off)"
        }
      }
    }
  }
}
