{
  "config": {
    "step": {
      "user": {
        "title": "Meal Solver 3000",
        "description": "Enable Meal Solver 3000 to manage the week's dinners."
      }
    },
    "abort": {
      "already_configured": "Meal Solver 3000 is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Rules & Settings",
        "data": {
          "max_rules":        "Max per week",
          "min_rules":        "Min per week",
          "no_consecutive":   "No consecutive side dish",
          "repeat_interval":  "Repeat interval (days)",
          "language":         "Card language",
          "auto_shuffle":     "Auto-shuffle",
          "shuffle_weekday":  "Shuffle day",
          "shuffle_time":     "Shuffle time (HH:MM)"
        },
        "data_description": {
          "max_rules":        "How many times a tag may appear per week. Format: tag:count — e.g. 'beef:2, fish:1' means max 2 beef dishes and 1 fish dish per week. Leave empty for no upper limit.",
          "min_rules":        "How many times a tag must appear per week. Format: tag:count — e.g. 'vegetarian:1' requires at least one vegetarian dish. Leave empty for no requirement.",
          "no_consecutive":   "Tags that cannot appear on back-to-back days. Format: comma-separated list — e.g. 'potato, rice, pasta' prevents the same side dish two days in a row. Leave empty to allow anything.",
          "repeat_interval":  "Days a dish must rest before it can be randomized again. E.g. 14 means the same dish won't appear for at least 2 weeks. Set to 0 to disable.",
          "language":         "Language used in the Lovelace card. sv = Svenska, en = English.",
          "auto_shuffle":     "When enabled, Meal Solver automatically generates a new week plan on the configured day and time. Disable if you prefer to shuffle manually only.",
          "shuffle_weekday":  "Day of the week when the automatic shuffle runs.",
          "shuffle_time":     "Time of day for the automatic shuffle in HH:MM format (e.g. 17:00)."
        }
      }
    }
  }
}
